version: "3.7"
services:
  jerotoma_sms:
    container_name: jerotoma_sms
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
    image: jerotoma_sms
    ports:
      - "8081:8080"
    networks:
      - jerotoma_sms_net
networks:
  jerotoma_sms_net:
    name: jerotoma_sms_net

