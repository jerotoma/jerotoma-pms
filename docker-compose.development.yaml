version: "3.7"
services:
  jerotoma_sms:
    container_name: jerotoma_sms    
    build:
      context: .
      dockerfile: Dockerfile.Development
    image: jerotoma_sms
    ports:
      - "8082:8080"
    networks:
      - jerotoma_sms_net
networks:
  jerotoma_sms_net:
    name: jerotoma_sms_net

